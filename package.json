{
  "name": "firestoreimport",
  "version": "1.0.0",
  "description": "nodejs program to import data to firebase firestore",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "deadpool",
  "license": "ISC",
  "dependencies": {
    "csvtojson": "^2.0.10",
    "firebase-admin": "^8.12.1"
  }
}
